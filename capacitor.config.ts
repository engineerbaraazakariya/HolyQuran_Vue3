import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.acrasoftware.HolyQuran',
  appName: 'القرآن الكريم',
  webDir: 'dist'
};

export default config;
